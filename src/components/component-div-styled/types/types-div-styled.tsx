export type ConfigTitle = {
    tagTitle?: string,
    colorTitle?: string,
    colorTitlePs?: boolean,
    sizeTitle?: string,
    paddingTitle?: string
};

export type ConfigText = {
    tagText?: string,
    colorText?: string,
    colorTextPs?: boolean,
    sizeText?: string,
    paddingText?: string
};

export type ConfigDiv = {
    colorDiv?: string,
    colorDivPs?: boolean,
    marginDiv?: string,
    paddingDiv?: string,
    widthDiv?: string,
    heightDiv?: string
};